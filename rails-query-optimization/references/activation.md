# Activation Guide

## Best-Fit Tasks
- Advanced Rails query optimization: diagnosing N+1 beyond simple includes, the cartesian product trap with multiple has_many eager loads, CTEs and lateral joins via Arel and raw SQL, reading EXPLAIN ANALYZE output, and counter cache patterns.
- Best trigger phrase: investigating slow Rails endpoints, optimizing ActiveRecord queries, or designing queries for high-traffic production endpoints.

## Explicit Invocation
- `Use $rails-query-optimization when investigating slow Rails endpoints, optimizing ActiveRecord queries, or designing queries for high-traffic production endpoints.`

## Cross-Agent Notes
- Start with `SKILL.md`, then load only the linked files you need.
- The standard metadata and this guide are portable across skills-compatible agents; Claude-specific frontmatter is optional and should degrade cleanly elsewhere.
