# Linux Install Runbook — {{machine_make_model}}

> Personalized runbook generated by `linux-install-planner`. Fill every
> `{{placeholder}}`. `/dev/sdX` and `/dev/nvme0n1pN` are placeholders — replace
> with the device you verify at the machine. Complete and confirm each phase
> before starting the next. Verify all versions against a current web search.

**Plan summary**
- Target machine: `{{machine_make_model}}` ({{laptop_or_desktop}})
- CPU / GPU(s): `{{cpu}}` / `{{gpu_vendors_models}}`
- Wi-Fi chipset: `{{wifi_chipset}}` ({{wifi_risk_note}})
- Distro: `{{distro_and_version}}` (from linux-distro-selector)
- Strategy: `{{full_wipe_or_dual_boot}}`  |  Encryption: `{{luks_yes_no}}`
- Filesystem: `{{ext4_or_btrfs_or_zfs}}`  |  Swap: `{{zram_or_swap}}`

---

## ⚠️ DANGER CHECKLIST — do these before anything else
- [ ] Personal data backed up to external drive AND cloud; **opened the copy and
      confirmed the files are there**. Windows recovery media created (if Windows
      may be wanted back).
- [ ] BitLocker turned off / decrypted (dual-boot) and **recovery key saved**.
- [ ] Storage mode known: `{{rst_vmd_or_ahci}}`. If switching RST→AHCI while
      keeping Windows, use the `bcdedit safeboot` dance.
- [ ] Fast Startup disabled (`powercfg /H off`) and machine **fully Shut Down**
      (not Restart).
- [ ] Target disk identified with `lsblk`: `{{target_disk}}` — confirmed by
      size/model before any flash or wipe.

---

## Phase 0 — End state
- [ ] Confirmed: {{full_wipe_or_dual_boot}}, encryption = {{luks_yes_no}}, distro =
      {{distro_and_version}}.

## Phase 1 — Pre-flight
- [ ] Backup verified (above).  - [ ] Windows recovery media made.
- [ ] Product key recorded: `(Get-CimInstance -ClassName SoftwareLicensingService).OA3xOriginalProductKey`
- [ ] Hardware recon captured (GPUs, Wi-Fi, storage soldered vs M.2, RST/AHCI,
      fingerprint/IR webcam).
- [ ] BIOS/UEFI updated from the vendor Windows tool.

## Phase 2 — Bootable media
- [ ] ISO checksum verified.  - [ ] ISO GPG signature = "Good signature".
- [ ] Writer: `{{ventoy_etcher_rufus_dd}}`. USB device confirmed: `{{usb_device}}`.
- [ ] ISO written / copied to the USB.

## Phase 3 — UEFI/BIOS + Windows prep
- [ ] `powercfg /H off` + full Shut Down.  - [ ] BitLocker handled + key saved.
- [ ] Firmware key: `{{firmware_key}}`; one-time boot menu key: `{{boot_menu_key}}`.
- [ ] Secure Boot: `{{keep_or_disable}}` ({{shim_or_arch_reason}}).
- [ ] RST/VMD handled if needed.  - [ ] TPM 2.0 left ON.

## Phase 4 — Partitioning, filesystem, LUKS
- [ ] Layout: ESP {{esp_size}} + root ({{fs}}) {{plus_home_or_not}}.
- [ ] Swap: {{zram_or_swap}} ({{swap_size_if_hibernate}}).
- [ ] Encryption: {{luks_choice}} (installer checkbox preferred).
- [ ] btrfs subvolumes + snapshot tool confirmed (if btrfs).

## Phase 5 — Installer walkthrough
- [ ] Booted USB via one-time menu; **tried the live session** (Wi-Fi, trackpad,
      display all OK).
- [ ] Network connected.  - [ ] Language/keyboard/timezone set.
- [ ] Third-party software checked (Ubuntu) / RPM Fusion planned (Fedora).
- [ ] Install type: `{{erase_alongside_manual}}` + Encrypt as chosen.
- [ ] Real hostname set: `{{hostname}}`.  - [ ] Installed; USB removed; rebooted.

## Phase 6 — First boot
- [ ] Full update run.  - [ ] Drivers installed (+ MOK enrolled if NVIDIA/Secure Boot).
- [ ] `fwupdmgr` firmware updates applied.  - [ ] Flatpak/Flathub (+ RPM Fusion) added.
- [ ] Exactly ONE power manager active.  - [ ] Hostname/timezone/locale confirmed.
- [ ] Fingerprint enrolled if supported.

## Phase 7 — Laptop fixes (only what applies)
- [ ] NVIDIA Optimus / PRIME configured: `{{prime_notes}}`.
- [ ] Wi-Fi firmware installed if needed: `{{wifi_fix}}`.
- [ ] Battery / power tuned.  - [ ] Suspend mode verified (`cat /sys/power/mem_sleep`).
- [ ] HiDPI / scaling set (Wayland preferred).

## Phase 8 — Reversibility
- [ ] Windows recovery media + install USB kept as rescue.
- [ ] LUKS header backed up off-machine (`cryptsetup luksHeaderBackup`).
- [ ] Noted: don't casually toggle RST↔AHCI or Secure Boot/TPM after install.

---

**Next:** with first boot working, switch to the `linux-dev-workstation` skill to
configure the dev environment, then `linux-ai-dev-stack` for the AI toolchain.
