# Activation Guide

## Best-Fit Tasks
- Client-side routing patterns using the History API — managing URL state without full page reloads, building provider-optional routing hooks, SSR-safe browser API access, scroll behavior, and parameter parsing. Covers both the architectural patterns (custom routechange events, popstate listeners) and practical hook design for zero-dependency routing libraries.
- Best trigger phrase: building or extending routing systems, debugging navigation bugs, or working with @page-speed/router patterns.

## Explicit Invocation
- `Use $client-side-routing-patterns when building or extending routing systems, debugging navigation bugs, or working with @page-speed/router patterns.`

## Cross-Agent Notes
- Start with `SKILL.md`, then load only the linked files you need.
- The standard metadata and this guide are portable across skills-compatible agents; Claude-specific frontmatter is optional and should degrade cleanly elsewhere.
