# AGENTS.md - `<subtree>` scope

This file adds subtree-specific instructions for AI coding agents working under `<subtree>`.

The parent `AGENTS.md` still applies. This file only captures the local rules that differ from or extend the parent.

## 1. Local mental model

- What this subtree owns
- How it fits into the parent repo

## 2. Local rules

1. Local contract or serialization rule
2. Local performance, safety, or architecture rule
3. Local verification requirement

## 3. Key files

- `...`
- `...`

## 4. Common workflows

- Update existing code in this subtree
- Add new code in this subtree

## 5. Verification

- `...`
