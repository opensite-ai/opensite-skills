# Activation Guide

## Best-Fit Tasks
- Senior-level async Rust patterns: Future Send bound failures, Rust 2024 lifetime capture rules, task cancellation with CancellationToken, blocking/async boundary design, and timeout composition.
- Best trigger phrase: debugging async compilation errors, structuring long-lived tasks, or designing services that mix async and CPU-bound work.

## Explicit Invocation
- `Use $rust-async-patterns when debugging async compilation errors, structuring long-lived tasks, or designing services that mix async and CPU-bound work.`

## Cross-Agent Notes
- Start with `SKILL.md`, then load only the linked files you need.
- The standard metadata and this guide are portable across skills-compatible agents; Claude-specific frontmatter is optional and should degrade cleanly elsewhere.
