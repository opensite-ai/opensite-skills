# Activation Guide

## Best-Fit Tasks
- React 19+ rendering performance: React Compiler diagnostics, profiler-driven optimization, useTransition for non-blocking updates, the new Activity and ViewTransition components, resource preloading APIs, and when to actually reach for useMemo/useCallback vs letting the Compiler handle it.
- Best trigger phrase: debugging slow renders, optimizing user-perceived performance, or implementing smooth navigation transitions in React 19+ applications.

## Explicit Invocation
- `Use $react-rendering-performance when debugging slow renders, optimizing user-perceived performance, or implementing smooth navigation transitions in React 19+ applications.`

## Cross-Agent Notes
- Start with `SKILL.md`, then load only the linked files you need.
- The standard metadata and this guide are portable across skills-compatible agents; Claude-specific frontmatter is optional and should degrade cleanly elsewhere.
